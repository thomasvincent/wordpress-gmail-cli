# This file is deprecated. Please use .github/workflows/codeql.yml instead.
# Keeping this file with 'on: workflow_dispatch' only to allow manual runs if needed.
name: DEPRECATED - CodeQL Analysis

on:
  workflow_dispatch:  # Manual trigger only

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Deprecated Workflow Notice
        run: |
          echo "⚠️ This workflow is deprecated. Please use the CodeQL Advanced workflow instead."
          echo "Redirecting to the new workflow..."
          
      - name: Trigger New Workflow
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'codeql.yml',
              ref: context.ref
            });
